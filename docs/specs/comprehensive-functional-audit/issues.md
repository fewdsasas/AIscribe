# 问题清单

## 问题统计

| 级别 | 数量 | 定义 |
|---|---|---|
| P0（致命） | 0 | 导致应用崩溃、数据丢失、主流程完全阻塞、敏感信息泄露 |
| P1（严重） | 18 | 核心数据完整性受损、跨模块状态不一致、关键校验缺失导致脏数据 |
| P2（一般） | 23 | 非核心功能异常、边界条件不完善、性能/规范/提示问题 |
| P3（轻微） | 0 | UI 细节、文案问题 |
| **总计** | **41** | |

## 按模块分布

| 模块 | P1 | P2 | 合计 |
|---|---|---|---|
| 环境基线 | 0 | 1 | 1 |
| 项目与小说 | 3 | 2 | 5 |
| 角色与世界观 | 3 | 3 | 6 |
| 大纲、情节结构与编辑器 | 3 | 3 | 6 |
| AI 对话与 LLM 配置 | 0 | 1 | 1 |
| 学习系统、技能系统与导入导出 | 4 | 8 | 12 |
| 数据库、持久化与 IPC | 5 | 3 | 8 |
| 跨模块协同 | 0 | 0 | 0 |

## 问题明细

| 问题编号 | 模块 | 功能点 | 严重度 | 复现步骤 | 实际结果 | 预期结果 | 截图/日志 | 修复建议 | 关联代码位置 | 状态 |
|---|---|---|---|---|---|---|---|---|---|---|
| ISSUE-001 | 环境基线 | 覆盖率检查 | P2 | 1. 运行 `npm run test:coverage`<br>2. 执行到 `tests/integration/stress-ipc.test.ts` scenario 3 | 测试超时失败：`Test timed out in 5000ms` | `test:coverage` 应完整执行并生成覆盖率报告 | `stress-ipc.test.ts:181` | 为 scenario 3 增加超时时间，或在 coverage 命令中排除压力测试 | `package.json` scripts / `vitest.config.ts` | Open |
| ISSUE-003-001 | 项目与小说 | `novel:create` 父项目 ID 校验 | P1 | 1. 调用 `novel:create` handler，传入 `{ projectId: 'invalid-id', title: 'Test' }` 或 `{ title: 'Test' }`（无 projectId）<br>2. 观察返回与数据库记录 | 返回成功创建的小说对象，`project_id` 被写入无效值或空字符串 | `projectId` 必填且必须为有效 UUID；缺失或无效时应抛出错误 | `findings-task-03.md` | 在 `novel:create` handler 中补充 `requireId(data.projectId, '项目ID')` | `src/main/ipc/novel.ipc.ts:11-16` | Open |
| ISSUE-003-002 | 项目与小说 | `chapter:create` 父小说 ID 校验 | P1 | 1. 调用 `chapter:create` handler，传入 `{ novelId: 'invalid-id', title: 'Ch1' }` 或 `{ title: 'Ch1' }`（无 novelId）<br>2. 观察返回与数据库记录 | 返回成功创建的章节对象，`novel_id` 被写入无效值或空字符串 | `novelId` 必填且必须为有效 UUID；缺失或无效时应抛出错误 | `findings-task-03.md` | 在 `chapter:create` handler 中补充 `requireId(data.novelId, '小说ID')` | `src/main/ipc/novel.ipc.ts:37-42` | Open |
| ISSUE-003-003 | 项目与小说 | `project:update` 名称非空校验 | P2 | 1. 创建一个项目<br>2. 调用 `project:update` handler，传入 `{ name: '' }` 或 `{ name: '   ' }` | 更新成功，项目名称在数据库中被置为 `NULL` 或保留空值 | 更新名称时应保持与 `project:create` 一致的非空约束，空/纯空白名称应报错 | `findings-task-03.md` | 在 `project:update` handler 中，当 `data.name` 存在时调用 `requireNonEmptyString(data.name, '项目名称')` | `src/main/ipc/project.ipc.ts:42-48`、`src/main/memory/repositories/project-repository.ts:78-96` | Open |
| ISSUE-003-004 | 项目与小说 | `chapter:update` 标题非空校验 | P2 | 1. 创建一个章节<br>2. 调用 `chapter:update` handler，传入 `{ title: '' }` 或 `{ title: '   ' }` | 更新成功，章节标题被置为空字符串 | 更新标题时应保持与 `chapter:create` 一致的非空约束，空/纯空白标题应报错 | `findings-task-03.md` | 在 `chapter:update` handler 中，当 `data.title` 存在时调用 `requireNonEmptyString(data.title, '章节标题')` | `src/main/ipc/novel.ipc.ts:78-85`、`src/main/memory/repositories/chapter-repository.ts:92-120` | Open |
| ISSUE-003-005 | 项目与小说 | `project:delete` 级联删除后的缓存一致性 | P1 | 1. 创建项目→小说→章节<br>2. 先通过 `chapter:get` 加载章节（使其进入 `ChapterRepository.cache`）<br>3. 删除所属项目<br>4. 再次调用 `chapter:get` | 由于数据库级联删除，章节已不存在，但 `ChapterRepository` 缓存未被失效，仍返回旧数据 | 级联删除应同步使子实体（novel/chapter）缓存失效，避免脏读 | `findings-task-03.md` | 在 `ProjectRepository.delete` 执行后，调用 `novelRepo.clearCache()` 与 `chapterRepo.clearCache()`；或在 `Database.deleteProject` 中统一清理相关仓库缓存 | `src/main/memory/repositories/project-repository.ts:98-101`、`src/main/memory/repositories/chapter-repository.ts:80-90`、`src/main/memory/repositories/base-repository.ts:31-33` | Open |
| ISSUE-004-001 | 角色与世界观 | 角色 personality/arc 反序列化会返回缺少必填字段的对象 | P1 | 1. 创建角色，只传入 `name` 与 `novelId`<br>2. 调用 `character:get` 读取角色<br>3. 访问 `character.personality.traits` | 返回的 `personality` 为 `{}`，访问 `traits` 得到 `undefined` | 即使数据库中存储默认空对象，反序列化后也应补齐 `PersonalityProfile` / `CharacterArc` 的所有必填字段 | `findings-task-04.md` | 反序列化时对对象进行 shape 校验，或使用 zod 等模式校验工具，确保返回对象包含所有必填字段；或在解析后与 fallback 做深度合并 | `src/main/memory/repositories/character-repository.ts:87-100` | Open |
| ISSUE-004-002 | 角色与世界观 | 角色关系 targetId 不存在时无任何校验或保护 | P1 | 1. 创建小说与角色 A<br>2. 调用 `character:create` 创建角色 B，传入 `relationships: [{ targetId: '<不存在的UUID>', type: 'friend' }]` | 创建成功，`relationships` 中保存了指向不存在角色的关系 | 应校验每个 `targetId` 是否存在于 `characters` 表，不存在时抛出明确业务错误 | `findings-task-04.md` | 在 `CharacterRepository.create` 中遍历 `relationships`，对每个 `targetId` 查询 `characters` 表确认存在性；或在 IPC 层增加校验逻辑 | `src/main/ipc/character.ipc.ts:10-17`、`src/main/memory/repositories/character-repository.ts:37-67` | Open |
| ISSUE-004-003 | 角色与世界观 | 世界观 powerSystem 反序列化会返回缺少必填字段的对象 | P1 | 1. 保存世界观时 `powerSystem` 只传 `{}`<br>2. 调用 `world:get-by-novel` 读取 | 返回的 `powerSystem` 为 `{}`，缺少 `name`、`rules`、`limitations`、`costs`、`source` | 反序列化后应包含 `PowerSystem` 的所有必填字段 | `findings-task-04.md` | 与 ISSUE-004-001 一致，对解析结果进行 shape 校验或与 fallback 合并必填字段 | `src/main/memory/repositories/world-repository.ts:88-90` | Open |
| ISSUE-004-004 | 角色与世界观 | `world:save` 在 IPC 层未校验必填字段 type | P2 | 1. 调用 `world:save` 传入 `{ novelId: '<uuid>', name: 'W' }`（无 type）或 `{ novelId: '<uuid>', name: 'W', type: 'invalid' }` | 保存成功，type 被写入默认值或非法字符串 | `type` 必填且应为合法 `WorldType` 枚举值 | `findings-task-04.md` | 在 IPC 处理器中校验 `data.type` 为非空字符串，并进一步校验其属于 `WorldType` 枚举值 | `src/main/ipc/world.ipc.ts:35-44`、`src/shared/types/ipc.ts:162-176` | Open |
| ISSUE-004-005 | 角色与世界观 | `character:create` 在 IPC 层未校验必填字段 role 与 novelId | P2 | 1. 调用 `character:create` 传入 `{ name: 'Alice' }` 或 `{ novelId: 'invalid', name: 'Alice' }` | 创建成功，`novel_id` 为空字符串或无效值；`role` 可能缺失并回退为 `'minor'` | `novelId` 必填 UUID，`role` 必填合法枚举值 | `findings-task-04.md` | 在 IPC 处理器中增加 `requireId(data.novelId, '小说ID')` 与 `role` 枚举值校验 | `src/main/ipc/character.ipc.ts:9-17`、`src/shared/types/ipc.ts:116-145` | Open |
| ISSUE-004-006 | 角色与世界观 | JSON 大字段缺乏长度上限校验 | P2 | 1. 调用 `character:create` 或 `world:save`，在 `personality` / `arc` / `geography` / `powerSystem` 等字段中传入 ≥50KB 的 JSON 字符串 | 保存成功，大字段被整体写入 SQLite `TEXT` 列；IPC payload 超过 1MB 时仅打印 warn | 单个 JSON 字段应设置合理长度上限，超限应拒绝或截断并提示 | `findings-task-04.md` | 在 Repository 或 IPC 层为 JSON 大字段增加长度上限校验（如单字段 ≤64KB），超限时抛错 | `src/main/ipc/character.ipc.ts`、`src/main/ipc/world.ipc.ts`、`src/main/ipc/index.ts:36-40` | Open |
| ISSUE-005-001 | 大纲、情节结构与编辑器 | `outline:save` 使用 `INSERT OR REPLACE` 导致更新时 `createdAt` 被覆盖且 `version` 未递增 | P1 | 1. 创建大纲并记录 `createdAt`<br>2. 调用 `outline:save` 更新内容<br>3. 调用 `outline:get` 读取 | 更新后 `createdAt` 被重置为当前时间，`version` 仍为 1 | 更新时应保留原始 `createdAt`，`version` 应递增 | `findings-task-05.md` | 区分 insert 与 update：update 时先读取现有记录，保留 `createdAt` 并更新 `updatedAt`；实现 `version` 自增 | `src/main/memory/repositories/outline-repository.ts:24-53`、`src/main/ipc/world.ipc.ts:54-61` | Open |
| ISSUE-005-002 | 大纲、情节结构与编辑器 | `plot-structure:save` 对 `beats.*.chapterIds` 未做存在性/外键校验 | P1 | 1. 创建小说与章节<br>2. 调用 `plot-structure:save` 传入 `beats: [{ chapterIds: ['<不存在章节ID>'] }]` | 保存成功，不存在的 `chapterIds` 被持久化 | 应校验每个 `chapterId` 是否存在于 `chapters` 表且属于当前 novel | `findings-task-05.md` | 在 `savePlotStructure` 中查询 `chapters` 表校验 `chapterIds`；不存在时抛出明确业务错误 | `src/main/memory/repositories/plot-structure-repository.ts:24-40`、`src/main/ipc/world.ipc.ts:17-25` | Open |
| ISSUE-005-003 | 大纲、情节结构与编辑器 | `outline` 与 `plot-structure` 仓库绕过 `BaseRepository.run` 和 `OperationLog` | P1 | 1. 调用 `outline:save` 或 `plot-structure:save`<br>2. 在 300ms 内强制关闭应用<br>3. 重新打开 DB | 最近的修改可能丢失，因为未记录 operation log | 所有写入应通过 `BaseRepository.run` 记录 operation log，保证 durability | `findings-task-05.md` | 将 `this.sqlDb.run(...)` 改为 `this.run(sql, params)`；或显式调用 `this._operationLog.append(sql, params)` 并清空缓存 | `src/main/memory/repositories/outline-repository.ts:37-51`、`src/main/memory/repositories/plot-structure-repository.ts:33-39`、`src/main/memory/repositories/base-repository.ts:53-58` | Open |
| ISSUE-005-004 | 大纲、情节结构与编辑器 | `NovelStructure.ts` 场景分隔输入规则缺少真实触发测试，且插入位置存在偏移风险 | P2 | 1. 在编辑器中输入 `--- ` 触发输入规则<br>2. 观察 sceneBlock 插入位置 | 测试文件未真实触发输入规则；实际 handler 使用 `tr.insert(start - 1, node)`，可能在段落内部插入 | sceneBlock 应替换分隔符并插入到正确位置 | `findings-task-05.md` | 补充真实输入规则测试；将 `tr.insert(start - 1, node)` 改为 `tr.insert(start, node)` | `src/renderer/components/editor/extensions/NovelStructure.ts:144-167`、`tests/renderer/components/novel-structure.test.ts:89-107` | Open |
| ISSUE-005-005 | 大纲、情节结构与编辑器 | `NovelEditor` 对超大内容、高频自动保存缺少显式防护 | P2 | 1. 向编辑器粘贴 10,000+ 字文本<br>2. 持续输入并观察性能与 IPC payload | 每次 `onUpdate` 都重新计算字数并触发 IPC 保存；无章节字数上限 | 应设置单章字数上限，对统计与保存增加防抖或分级处理 | `findings-task-05.md` | 为单章内容设置上限；对 `onUpdate` 增加防抖；提升 IPC payload warn 阈值为可配置 hard limit | `src/renderer/components/editor/NovelEditor.tsx:112-120`、`src/main/ipc/index.ts:36-40` | Open |
| ISSUE-005-006 | 大纲、情节结构与编辑器 | `world.ipc.ts` 对 `outline` / `plot-structure` 保存数据的内容校验不足 | P2 | 1. 调用 `plot-structure:save` 传入非法 `framework` 或缺少 `beats` 必填字段<br>2. 调用 `outline:save` 传入非法 `type` | 保存成功，非法值被持久化 | 应校验 `framework`、`type`、`beats.*.status`、`structure.*.phase` 等枚举与结构 | `findings-task-05.md` | 在 IPC 处理器中增加枚举与数组结构校验；或在仓库反序列化时做运行时 shape 校验 | `src/main/ipc/world.ipc.ts:17-25`、`src/main/ipc/world.ipc.ts:54-61`、`src/shared/types/ipc.ts:147-160`、`src/shared/types/ipc.ts:178-191` | Open |
| ISSUE-006-001 | AI 对话与 LLM 配置 | 非流式 API 错误前缀与放行守卫不一致 | P2 | 1. 触发一次非 2xx 的 LLM 请求（如 401）<br>2. 观察 `executeChatRequest` 抛出的错误 | 抛出的错误前缀为中文 `API 错误 (...)`，放行守卫检查英文 `API Error`，导致被二次包装 | 应统一错误前缀，避免不必要的二次包装 | `findings-task-06.md` | 将 `API 错误` 改为 `API Error`，或在放行守卫同时兼容中文前缀 | `src/main/engine/llm-provider.ts:338`、`src/main/engine/llm-provider.ts:345` | Open |
| ISSUE-007-001 | 学习系统、技能系统与导入导出 | `learning:record` IPC handler 未校验必填字段 | P1 | 1. 调用 `learning:record` 传入 `{ projectId: '<uuid>', query: 'q' }`（无 sessionId/response/duration） | 记录成功，缺失字段被写入默认值（空字符串/0） | `sessionId`、`response`、`duration` 应按类型校验 | `findings-task-07.md` | 补充 `requireId(data.sessionId, '会话ID')`、`requireNonEmptyString(data.response, '响应')`、数值/非负校验 | `src/main/ipc/learning.ipc.ts:11-19` | Open |
| ISSUE-007-002 | 学习系统、技能系统与导入导出 | skill-loader 使用正则解析 YAML frontmatter，健壮性不足 | P2 | 1. 创建 SKILL.md，frontmatter 中使用引号 `name: 'novel-master'` 或多行 description<br>2. 启动应用调用 `skill:list` | 解析结果可能错误或不完整 | 应使用 YAML 解析器正确解析 frontmatter | `findings-task-07.md` | 引入 `yaml` 或 `js-yaml` 作为依赖，使用 `YAML.parse(frontmatter)` 解析；显式捕获 YAML 语法错误 | `src/main/engine/skill-loader.ts:141-165` | Open |
| ISSUE-007-003 | 学习系统、技能系统与导入导出 | skill-loader 初次加载失败后不会重试，且失败静默 | P2 | 1. 临时删除或损坏 `skills/` 目录<br>2. 启动应用<br>3. 恢复目录后再次调用 `skill:list` | 初次加载失败后 `loaded = true`，后续不再重试；损坏文件仅 warn | 加载失败后应允许重试；应记录加载摘要 | `findings-task-07.md` | 将 `this.loaded = true` 移到成功读取并解析至少一个目录之后；记录加载结果摘要 | `src/main/engine/skill-loader.ts:35-47`、`src/main/engine/skill-loader.ts:50-77` | Open |
| ISSUE-007-004 | 学习系统、技能系统与导入导出 | skill-loader 硬编码 skills 目录路径，缺少兜底告警 | P2 | 1. 将应用打包到非标准结构，使两个硬编码候选路径均不存在<br>2. 启动应用调用 `skill:list` | 返回空数组，无错误无告警 | 当所有候选目录都不存在时，应抛出错误或记录 error 级别告警 | `findings-task-07.md` | 当候选目录均不存在时抛出带路径信息的错误或记录 error 日志 | `src/main/engine/skill-loader.ts:40-46` | Open |
| ISSUE-007-005 | 学习系统、技能系统与导入导出 | 重复 name 的技能会静默覆盖 | P2 | 1. 在多个子目录中放置 `name` 相同的 SKILL.md<br>2. 调用 `skill:list` / `skill:get` | 后加载的技能静默覆盖先加载的技能 | 应检测重复并记录 warn，或采用唯一键 | `findings-task-07.md` | 在 `this.skills.set(skill.name, skill)` 前检查是否已存在，存在则记录 warn | `src/main/engine/skill-loader.ts:69`、`src/main/engine/skill-loader.ts:110`、`src/main/engine/skill-loader.ts:132` | Open |
| ISSUE-007-006 | 学习系统、技能系统与导入导出 | `skill:get` 与 `skill:invoke` 对不存在技能的处理不一致 | P2 | 1. 调用 `skill:get` 传入不存在技能名<br>2. 调用 `skill:invoke` 传入不存在技能名 | `skill:get` 返回 `null`，`skill:invoke` 抛出 `Error('技能不存在: ${name}')` | 同一领域应统一错误契约 | `findings-task-07.md` | 统一行为：`skill:invoke` 在 handler 层先检查，不存在时返回一致的错误结果；或两者都抛出可识别异常 | `src/main/ipc/skill.ipc.ts:15-23`、`src/main/engine/skill-loader.ts:211-215` | Open |
| ISSUE-007-007 | 学习系统、技能系统与导入导出 | `learning:summary` 的 `totalInteractions` 与 `lastActive` 语义错误 | P1 | 1. 记录多条学习轨迹<br>2. 调用 `learning:summary` | `totalInteractions` 最多为 500（注释承认是下限）；`lastActive` 取最早记录时间 | `totalInteractions` 应为精确计数；`lastActive` 应为最近活跃时间 | `findings-task-07.md` | 使用 `COUNT(*)` 返回精确总数；将查询改为 `ORDER BY timestamp DESC LIMIT 1` | `src/main/learning/engine.ts:73-92`、`src/main/learning/trajectory.ts`、`src/main/memory/repositories/trajectory-repository.ts:65-72` | Open |
| ISSUE-007-008 | 学习系统、技能系统与导入导出 | `WriterModelUpdater` 将 `projectId` 误作 `writerId` | P1 | 1. 在多个项目记录轨迹<br>2. 调用 `writer-model:get` | 每个项目生成不同的 writer profile | writerId 应来自真实用户/作者标识，或按项目分析时重命名字段 | `findings-task-07.md` | writerId 应来自固定 machine id 或用户账号；若只能按项目分析，重命名为 `projectId` | `src/main/learning/writer-model.ts:5-7` | Open |
| ISSUE-007-009 | 学习系统、技能系统与导入导出 | ExportEngine 对损坏章节内容的回退策略会导致元数据泄漏到正文 | P1 | 1. 构造一个 JSON 解析失败的章节内容<br>2. 调用 `export:project` | 导出结果包含 `"type":"doc"` 等元数据 | 应剥离 JSON 标记或记录损坏提示，不泄露元数据 | `findings-task-07.md` | 回退时尝试剥离 JSON 标记，或记录 warn 日志；补充损坏内容导出测试 | `src/main/export/index.ts:34-43` | Open |
| ISSUE-007-010 | 学习系统、技能系统与导入导出 | HTML 导出未按段落拆分，可能合并多段文本 | P1 | 1. 创建含多个段落的章节<br>2. 以 HTML 格式导出 | 相邻段落被合并为单个 `<p>` | 每个段落应生成独立的 `<p>` | `findings-task-07.md` | 在 `extractTipTapText` 中段落之间插入 `\n`，确保 HTML 导出保留段落结构 | `src/main/export/index.ts:139-145`、`src/main/export/index.ts:49` | Open |
| ISSUE-007-011 | 学习系统、技能系统与导入导出 | skill/learning/export IPC handler 使用 `IPC_CHANNELS` 常量，违反项目约定 | P2 | 1. 查看 `src/main/ipc/skill.ipc.ts`、`learning.ipc.ts`、`export.ipc.ts` 及 `src/preload/index.ts` | 均从 `../../shared/types/ipc` 导入 `IPC_CHANNELS` 并使用 | 应内联定义通道字符串，不使用 `IPC_CHANNELS` 常量 | `findings-task-07.md` | 内联定义通道字符串，移除对 `IPC_CHANNELS` 的依赖；或单独提全局重构任务 | `src/main/ipc/skill.ipc.ts`、`src/main/ipc/learning.ipc.ts`、`src/main/ipc/export.ipc.ts`、`src/preload/index.ts` | Open |
| ISSUE-007-012 | 学习系统、技能系统与导入导出 | `learning:record` 未对 `context` 对象大小做限制 | P2 | 1. 调用 `learning:record` 传入超大 `context` 对象（如整章内容） | 成功写入，可能导致数据库膨胀与 IPC payload 过大 | 应限制 `context` 大小/深度 | `findings-task-07.md` | 增加 `context` 大小/深度限制（如序列化后 ≤64KB），超限时截断并记录 warn | `src/main/ipc/learning.ipc.ts:10-18` | Open |
| ISSUE-008-001 | 数据库、持久化与 IPC | `novel:create` 父项目 ID 校验 | P1 | 1. 调用 handler 传入 `{ title: 'Test' }` 或 `{ projectId: 'invalid', title: 'Test' }` | 创建成功，`project_id` 被写入空字符串或无效值 | `projectId` 必填且为有效 UUID | `findings-task-08.md` | 补充 `requireId(data.projectId, '项目ID')` | `src/main/ipc/novel.ipc.ts:11-16` | Open |
| ISSUE-008-002 | 数据库、持久化与 IPC | `chapter:create` 父小说 ID 校验 | P1 | 1. 调用 handler 传入 `{ title: 'Ch1' }` 或 `{ novelId: 'invalid', title: 'Ch1' }` | 创建成功，`novel_id` 被写入空字符串或无效值 | `novelId` 必填且为有效 UUID | `findings-task-08.md` | 补充 `requireId(data.novelId, '小说ID')` | `src/main/ipc/novel.ipc.ts:37-42` | Open |
| ISSUE-008-003 | 数据库、持久化与 IPC | `character:create` novelId 与 role 校验 | P1 | 1. 传入 `{ name: 'Alice' }` 或 `{ novelId: 'invalid', name: 'Alice' }` | 创建成功，`novel_id` 为空字符串或无效值；`role` 可能缺失 | `novelId` 必填 UUID，`role` 必填 | `findings-task-08.md` | 补充 `requireId(data.novelId, '小说ID')` 与 `requireNonEmptyString(data.role, '角色类型')` | `src/main/ipc/character.ipc.ts:11-16` | Open |
| ISSUE-008-004 | 数据库、持久化与 IPC | `checkpoint:create` 项目 ID 校验 | P1 | 1. 传入 `{ label: 'v1' }` 或 `{ projectId: 'invalid', label: 'v1' }` | 创建成功，`project_id` 为空字符串或无效值 | `projectId` 必填且为有效 UUID | `findings-task-08.md` | 补充 `requireId(data.projectId, '项目ID')` | `src/main/ipc/checkpoint.ipc.ts:11-16` | Open |
| ISSUE-008-005 | 数据库、持久化与 IPC | `session:create` 项目 ID 校验 | P1 | 1. 传入 `{}` 或 `{ projectId: 'invalid' }` | 创建成功，`project_id` 为空字符串或无效值 | `projectId` 必填且为有效 UUID | `findings-task-08.md` | 补充 `requireId(data.projectId, '项目ID')` | `src/main/ipc/checkpoint.ipc.ts:37-42` | Open |
| ISSUE-008-006 | 数据库、持久化与 IPC | `learning:record` 必填字段校验 | P1 | 1. 传入 `{ projectId: '<uuid>', query: 'q' }`（无 sessionId/response/duration） | 记录成功，缺失字段被写入默认值（空字符串/0） | `sessionId`、`response`、`duration` 应按类型校验 | `findings-task-08.md` | 补充 `requireNonEmptyString(data.sessionId, '会话ID')`、`requireNonEmptyString(data.response, '响应')`、数值/非负校验 | `src/main/ipc/learning.ipc.ts:11-19` | Open |
| ISSUE-008-007 | 数据库、持久化与 IPC | `project:update` 与 `chapter:update` 名称非空校验 | P2 | 1. 更新时传入 `{ name: '' }` / `{ title: '   ' }` | 更新成功，名称/标题被置为空或空白 | 更新操作应保持创建时的非空约束 | `findings-task-08.md` | 存在 `data.name`/`data.title` 时调用 `requireNonEmptyString` | `src/main/ipc/project.ipc.ts:42-48`、`src/main/ipc/novel.ipc.ts:78-85` | Open |
| ISSUE-008-008 | 数据库、持久化与 IPC | `world:save` 与 `outline:save` type 枚举校验 | P2 | 1. 传入 `{ novelId: '<uuid>', name: 'W' }`（无 type）或 `{ novelId: '<uuid>', type: 'invalid' }` | 保存成功，type 被写入默认值或非法字符串 | `type` 必填且应为 `'brief' \| 'detailed'`（outline）或合法 `WorldType` | `findings-task-08.md` | 补充枚举校验，非法值抛错 | `src/main/ipc/world.ipc.ts:37-43`、`src/main/ipc/world.ipc.ts:56-61` | Open |
