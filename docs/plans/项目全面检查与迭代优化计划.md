# 项目全面检查与迭代优化计划

## 一、摘要

对 AIscribe（Electron + React + SQLite 小说创作平台）进行全面代码质量评估、功能测试验证、性能测量与优化，并制定可执行的迭代计划，最终输出详细迭代报告。

## 二、当前状态分析

### 2.1 项目概况

| 维度 | 数值 |
|------|------|
| 项目类型 | Electron + React + TypeScript + sql.js WASM |
| 源代码 | 108 文件 / 404 KB |
| 运行时依赖 | 5 个（精简） |
| 架构 | 三进程：main（Node.js）/ preload（桥接）/ renderer（React SPA） |
| IPC 通道 | 12 个 handler 文件，约 46+ 个通道 |
| 数据库 | sql.js WASM，版本化迁移，schema_version=3 |

### 2.2 已完成的工作

**代码质量修复（上一轮完成）**：
- TypeScript 错误：38 → 0（缺失导入、未使用变量、protected 成员访问、类型错误等）
- ESLint 错误：4 → 0（动态 `import()` 类型注解、`import type` 规范）
- 测试：404 个全部通过，无回归
- 修复涉及：14 个文件修改

### 2.3 仍需解决的问题

| 类别 | 问题描述 | 严重程度 |
|------|---------|---------|
| 测试覆盖率 | 整体 54.59%，远低于 80% 目标 | 高 |
| 覆盖率 - Renderer | AIChatView 等 10+ 组件覆盖率 0% | 高 |
| 覆盖率 - Engine | llm-provider (36.25%), skill-loader (47.16%) | 高 |
| 覆盖率 - IPC | project/novel/chapter IPC 通道 30-50% | 中 |
| ESLint 警告 | 249 个（sort-imports + no-non-null-assertion） | 中 |
| Prettier | 136 个文件未格式化 | 中 |
| IPC 测试缺口 | 约 30% 通道无 IPC 级测试 | 中 |

## 三、执行计划

### 阶段 1：代码质量评估（已完成）

| 步骤 | 状态 | 说明 |
|------|------|------|
| 1.1 TypeScript 静态分析 | 已完成 | 38 错误均修复 |
| 1.2 单元测试覆盖率检查 | 已完成 | 54.59%，缺口已分析 |
| 1.3 代码规范验证 | 已完成 | ESLint 0 错误/249 警告，Prettier 136 文件需格式化 |

### 阶段 2：功能测试验证（条件已具备）

**分析结论**（来自上一轮的深度分析）：
- 60 个测试文件 / 404 个测试全部通过
- IPC handler 覆盖矩阵已分析：最好（storage 11 测试）到最差（novel IPC 30%）
- 识别出 12 个边缘场景缺口：
  - **高优先级**：sanitizeError 无测试、handleIPCError 无测试、MAX_STRING_LENGTH 边界测试缺失、chat-stream onError 未验证
  - **中优先级**：project/novel/chapter IPC 通道缺口、outline/plot-structure 缺口
  - **低优先级**：null/undefined 输入测试、renderer API 失败反馈测试

### 阶段 3：性能测量与优化

**已测量指标**：

| 指标 | 数值 | 目标 | 判定 |
|------|------|------|------|
| TypeScript 编译时间 | 3.5s | < 10s | 通过 |
| 纯测试执行时间 | ~1.2s | < 5s | 通过 |
| 含环境测试执行 | ~27s | < 60s | 通过 |
| 源码体积 | 404 KB | - | 适中 |
| node_modules 包数 | 523 | - | 正常 |
| 运行时依赖 | 5 个 | 精简 | 优秀 |
| 最大文件 | database.ts (16.8KB) | - | 适中 |

### 阶段 4：制定迭代计划（已产出）

完整迭代报告已写入 `迭代报告.md`，包含三阶段迭代计划：
- **第一迭代（高优先级，~24h）**：核心安全测试、IPC handler 补全、Renderer 核心组件测试
- **第二迭代（中优先级，~6h）**：outline/plot-structure IPC、null/undefined 输入测试、Prettier 格式化
- **第三迭代（低优先级，~15h）**：大文件拆分、集成测试 IPC 路径、无覆盖组件基础测试

## 四、提议的变更

### 变更 1：修复 249 个 ESLint 警告
- **文件范围**：`src/**/*.{ts,tsx}` + `tests/**/*.{ts,tsx}`
- **操作**：运行 `npm run lint:fix` 自动修复 105 个可自动修复的警告
- **手动修复**：`no-non-null-assertion` 警告需要用可选链或类型守卫替代

### 变更 2：运行 Prettier 格式化全部代码
- **文件范围**：所有 `src/` 和 `tests/` 下 136 个文件
- **操作**：运行 `npm run format`
- **影响**：纯格式变更，不影响功能

### 变更 3：补充测试覆盖率
- **高优先级**：
  - 增加 `sanitizeError`/`handleIPCError` 测试（`index.ipc.ts`）
  - 增加 `MAX_STRING_LENGTH` 边界测试
  - 增加 `chat-stream` onError 路径测试
  - 补全 `project:update/delete/dashboard-stats` IPC 测试
  - 补全 `novel:get/get-by-project`、`chapter:list/get/update/counts` IPC 测试
- **中优先级**：
  - 补全 `outline:get/save`、`plot-structure:get-by-novel/save` IPC 测试
  - 所有 handler 增加 null/undefined 输入测试

### 变更 4：更新迭代报告
- 将变更 1-3 的状态合并到 `迭代报告.md`
- 新增优化效果对比章节

## 五、假设与决策

| 假设/决策 | 说明 |
|-----------|------|
| 优先保证已有测试不回归 | 所有变更需通过 `npm run test:run` 验证 |
| ESLint 警告逐步修复 | non-null-assertion 遍布 100+ 处，需逐个人工审查 |
| 不主动创建文档文件 | 遵循项目规则，仅修改 `迭代报告.md` |
| 不做架构性重写 | coverage 提升通过增加测试，不重构生产代码 |

## 六、验证步骤

1. **TypeScript 验证**：`npm run typecheck` → 0 错误
2. **ESLint 验证**：`npm run lint` → 0 错误
3. **测试验证**：`npm run test:run` → 全部通过
4. **覆盖率验证**：`npm run test:coverage` → 覆盖率提升
5. **格式验证**：`npm run format:check` → 无格式问题

## 七、文件影响范围

| 文件/目录 | 变更类型 | 说明 |
|-----------|---------|------|
| `tests/main/ipc/*.test.ts` | 新增/修改 | 补充 IPC handler 测试 |
| `tests/main/memory/repositories/*.test.ts` | 新增 | 补充仓库测试 |
| `src/**/*.{ts,tsx}` | 格式化 | Prettier 格式化 |
| `tests/**/*.{ts,tsx}` | 格式化 | Prettier 格式化 |
| `迭代报告.md` | 更新 | 追加优化结果 |
